export default function Home() {
  return (
    <section className="mx-auto flex max-w-7xl flex-col items-start gap-4 px-6 py-32">
      <p className="text-sm font-semibold uppercase tracking-widest text-ink/50">
        Foundation stage
      </p>
      <h1 className="font-display text-5xl font-extrabold tracking-tight sm:text-6xl">
        Homepage build starts here.
      </h1>
      <p className="max-w-xl text-lg text-ink/70">
        Layout, fonts, tokens, and the Shopify connection are wired up. Real
        sections come next, one at a time.
      </p>
    </section>
  );
}
